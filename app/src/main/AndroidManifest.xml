<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.terminatingcode.android.migrainetree"
          xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.flash"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".jwetherell_heart_rate_monitor.HeartRateMonitor"
                  android:configChanges="keyboardHidden|orientation"
                  android:screenOrientation="portrait">
        </activity>

        <provider
            android:authorities="com.terminatingcode.android.migrainetree.Calendar.provider"
            android:name=".SQL.MenstrualRecordsProvider"
            android:exported="false">
        </provider>

        <service
            android:name=".Weather.GeoLookupService"
            android:exported="false">
        </service>

        <service
            android:name=".Weather.WeatherHistoryService"
            android:exported="false">
        </service>
    </application>

</manifest>